// Minimal original language specification for tests.
// Not derived from any game's nwscript.nss.

int TRUE = 1;
int FALSE = 0;

void PrintString(string sString);
int Random(int nMaxInteger);
object GetModule();
void DestroyObject(object oDestroy);
